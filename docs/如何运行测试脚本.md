# 如何运行测试脚本

## ⚠️ 重要说明

项目中的测试脚本（`scripts/test_*.py`）**不是pytest测试文件**，它们是普通的Python脚本，需要直接运行。

## 🚀 正确的运行方式

### 方式1：命令行运行（推荐）

```bash
# 在项目根目录运行
python scripts/test_llm_api.py
python scripts/test_api_quick.py
python scripts/test_api_service.py
```

### 方式2：在PyCharm中运行

**不要使用pytest运行器！**

正确步骤：
1. 右键点击脚本文件（例如 `test_llm_api.py`）
2. 选择 **"Run 'test_llm_api'"** 或 **"运行 'test_llm_api'"`
3. **不要**选择 "Run pytest in test_llm_api.py"

或者：
1. 打开脚本文件
2. 点击文件右上角的运行按钮（绿色三角形）
3. 选择 "Run 'test_llm_api'"

### 方式3：配置PyCharm运行配置

1. 打开 **Run** → **Edit Configurations...**
2. 如果看到pytest配置，**删除它**
3. 创建新的Python配置：
   - Name: `test_llm_api`
   - Script path: `C:\Users\Yu\cursorProjects\githup\scripts\test_llm_api.py`
   - Working directory: `C:\Users\Yu\cursorProjects\githup`
   - Python interpreter: 选择您的Python解释器

## 📋 可用的测试脚本

### 1. 快速API测试
```bash
python scripts/test_api_quick.py
```
- 快速测试LLM API配置
- 验证API是否可以正常调用

### 2. 完整LLM测试
```bash
python scripts/test_llm_api.py
```
- 测试LLM API配置
- 测试AI服务集成
- 测试情感分析、关键词提取、LLM深度分析

### 3. API服务测试
```bash
python scripts/test_api_service.py
```
- 测试API服务是否运行
- 测试所有主要API端点
- 创建测试数据

### 4. 查找可用模型
```bash
python scripts/list_available_models.py
```
- 从API获取可用模型列表
- 测试常见模型名称

## 🔧 如果PyCharm仍然尝试用pytest运行

### 方法1：排除scripts目录

1. 打开 **File** → **Settings** → **Tools** → **Python Integrated Tools**
2. 在 **Testing** 部分：
   - 取消勾选 "Auto-detection of test frameworks"
   - 或者设置 "Test runner" 为 "Unittests"
   - 在 "Pattern" 中排除 `scripts/test_*.py`

### 方法2：重命名文件（不推荐）

如果必须使用pytest，可以创建真正的pytest测试文件：
- 文件名：`tests/test_llm_api.py`
- 使用 `def test_*()` 函数
- 使用 `pytest` 命令运行

### 方法3：添加pytest.ini配置

已在 `scripts/pytest.ini` 中配置，但建议：
- 将真正的pytest测试放在 `tests/` 目录
- `scripts/` 目录中的文件作为工具脚本

## ✅ 验证运行方式

运行后应该看到类似输出：
```
================================================================================
LLM聚合网关API测试
================================================================================
...
✅ API调用成功！
...
```

**不应该**看到：
```
============================= test session starts =============================
collecting ... collected 0 items
```

## 📝 总结

- ✅ **正确**：`python scripts/test_llm_api.py`
- ❌ **错误**：`pytest scripts/test_llm_api.py`
- ❌ **错误**：在PyCharm中使用pytest运行器

这些脚本是**工具脚本**，不是**单元测试**，需要直接运行Python解释器。


